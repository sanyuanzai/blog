import React from 'react'
import type { FC, ReactNode } from 'react'
interface Iprops {
  children?: ReactNode
}

const MessageBoard: FC<Iprops> = () => {
  return <div>MessageBoard</div>
}
export default MessageBoard

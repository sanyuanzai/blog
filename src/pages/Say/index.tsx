import React from 'react'
import type { FC, ReactNode } from 'react'
interface Iprops {
  children?: ReactNode
}

const Say: FC<Iprops> = () => {
  return <div>Say</div>
}
export default Say
